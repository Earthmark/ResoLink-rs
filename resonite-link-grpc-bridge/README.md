# Resonite Link GRPC Bridge

GRPC proxy for the resolink protocol.

```bash
resonite-link-grpc-bridge -r ws://localhost:18229 -g 127.0.0.1:8080
```

This bridge will currently break on many different kinds of fields, especially nullable ones.
